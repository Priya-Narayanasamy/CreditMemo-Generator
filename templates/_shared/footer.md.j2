---

### Provenance

Every figure in this memo is drawn from the evidence ledger for
{{ application_number }}. {{ ledger_size }} values were sourced: {{ database_count }}
from the application database, {{ document_count }} from supporting documents and
{{ computed_count }} computed by the calculators from other ledger values. No figure
appears in this memo that is not in the ledger with a resolved source.

| | |
|---|---|
| Application | {{ application_number }} |
| Template | {{ template_id }} |
| Policy ruleset | {{ policy_version }} |
| Extraction model | {{ models.extraction }} |
| Drafting model | {{ models.drafting }} |
| Review model | {{ models.review }} |
| Generated | {{ generated_at }} |
| Approved by | {{ approved_by }} |

This is a first draft prepared for a credit analyst. It is not a credit decision,
an assessment, or a recommendation.
